machine:
  services:
    - docker

test:
  override:
    - echo temporarily disabled
    - # make short-test

deployment:
  push_to_dockerhub:
    branch: circle-release
    commands:
      - make release
